/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'api.gycoding.com',
      'localhost',
      'lh3.googleusercontent.com',
      'assets.hardcover.app',
    ],
  },
};

export default nextConfig;
