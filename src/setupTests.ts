import '@testing-library/jest-dom';
// This file is picked up by Jest via setupFilesAfterEnv (jest.setup.js already requires jest-dom),
// but having this file helps TypeScript find the types when referenced in tsconfig "types" or the test environment.
